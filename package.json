{
  "name": "supercar-finance",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cross-env PORT=3000 npm run dev --prefix frontend\" \"cross-env PORT=3001 npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "concurrently \"cross-env PORT=3000 npm start --prefix frontend\" \"cross-env PORT=3001 npm start --prefix backend\"",
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
